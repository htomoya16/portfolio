// Timeline data — reserved for future use
// The About section currently uses hardcoded card data in AboutSection.tsx

export interface TimelineItem {
  year: string
  label: string
}

export const timeline: TimelineItem[] = []
